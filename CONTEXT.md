# Career Nova22

本地优先、单用户、开源的求职桌面应用。帮求职者准备简历与求职。

> 这是全项目的**术语词典**（跨功能通用语言）。功能分区的设计决策与理由见 `docs/design/<分区>.md`。
> 本文件只定义"词是什么"，不记"决策怎么做"——后者归分区文档。

## Language

**用户信息事实库 (user_facts)**:
存"对用户的了解"的原料——自然语言事实 + 基本分类，全局共享（简历/优化/生成/市场调研都从这里取材）。是原料不是成品；整理渲染成简历是下游 agent 的活。
_Avoid_: 结构化简历、简历表、用户信息表

**事实 (fact)**:
事实库里的一条记录，是对用户某方面的一句自然语言描述（如"2021–2024 南京大学软件工程本科"）。
_Avoid_: 字段、条目、记录项

**上简历标记 (on_resume)**:
事实的一个布尔维度——**这条 active 事实该不该渲染进简历成品**。默认 True（内容类事实都上）；False 用于「需要知道但不上简历」的事实（求职方向、敏感信息、想淡化的经历）。**LLM 在记录/抽取时默认判一次，人可在信息库页开关否决；落库固定、不每次重判**。事实库是唯一真相源——删除事实 = 标 superseded，不是给简历挂「假删除」标记。
_Avoid_: 删除标记、隐藏字段（假性删除）、方向特例前缀（散在 prompt 里的补丁）

**机器门 (machine gate)**:
改简历流水线（生成/改写/**开始改**都走这一条）里 content 之后的**确定性校验**——「每个 active 且 on_resume 的事实都在成品里」。漏了沿 `content_problems` 喂回 content 自动补（`MAX_ITERATIONS` 兜底），**不需要人**。只拦「整条事实连影子都没进成品」的硬丢失；软质量（措辞/量化够不够）是 LLM 的活，不归它。
_Avoid_: 软质量校验、LLM 对账门

**人门 (human gate)**:
改简历流水线（生成/改写/**开始改**都走这一条）最后的**主观确认**——草稿渲染好后人拍板：确认→落库 / 带意见重改→回 content。用 `interrupt()` 挂起等拍板，和机器门（确定性、无人）是**两道性质不同的门**。拍板走独立 UI + 端点，不进聊天循环。
_Avoid_: 聊天内确认、自动落库

**编排检查点 (orchestration checkpoint)**:
LangGraph checkpointer 持久化的图挂起状态（人门/抽取确认的中断现场）。属**编排基础设施**，不违反「Graph 不碰 DB」红线——节点不碰它，真正写业务库的仍是 service 层。用 `AsyncSqliteSaver` 落**独立 checkpoint 文件**（与业务库分离），挂起状态天然抗重启；thread_id 按 `{graph}:{scope}` **派生**（单用户单值），不落库、不管理。
_Avoid_: 存进业务库、持久化 thread_id 进 settings 表

**基本分类 (category)**:
事实的粗归类，非硬 schema：基本信息 / 教育经历 / 工作经历 / 项目经历 / 个人技能（含证书）/ 其他补充。
_Avoid_: 框架、schema、结构

**聊天 (chat)**:
贯穿始终的统一基础动作。有无简历都通过聊天搜集事实；区别只在最后是"从无到有生成"还是"基于现有生成新版"。上传简历是聊天流里的一个大批量采集动作，不是独立入口。
_Avoid_: 识别入口、上传流程（作为独立概念）

**简历文档 (resume_document)**:
简历的**成品/范本**——版本化 Markdown。v1 是上传文件经 MarkItDown 转出，之后 LLM 从事实库取材组合产出新版本。**是事实的一次投影**（排版+措辞+组装后的事实），不是事实真相源——源数据在 `user_facts`。
_Avoid_: 简历表、事实库（它是成品不是知识）

**后台抽取 (background extraction)**:
上传简历转文档后，后台异步跑识别图，结果进左栏「抽取信息卡片」供确认。**LLM 不在上传关键路径**——上传毫秒级返回，抽取不阻塞聊天。抽取结果**确认才入库**（source=resume_upload）；拒绝则该批不入库、通道重开可重传。
_Avoid_: 上传识别（作为阻塞步骤）

**抽取信息卡片 (extraction card)**:
后台抽取完成后，左栏弹出的**可编辑事实摘要**（按基本分类分组、总分两段式）。确认 = 卡片事实统一冲突检测后入库 + 关闭上传通道；拒绝 = 该批不入库、通道重开。**候选不入库**——抽取结果先落卡片（可编辑），确认后才入库。
_Avoid_: 确认卡片（旧版只读工作台）、审批单

**版本化回滚 (versioned rollback)**:
简历文档 v1/v2/… 线性版本。当前实现 = **软作废回滚**（2026-08-10 起，取代旧的"纯追加"）：回滚把目标稿之后的版本标 `superseded`、目标稿恢复为当前，**不删历史**。事实快照与版本同代，回滚可选连事实一起还原。设计与理由 → [`docs/design/resume.md`](docs/design/resume.md) §9/§9bis。
_Avoid_: Git 式多分支（单用户线性够用，真分支已否决 2026-08-31）、纯追加复制（旧实现，已取代）

**简历 agent**:
唯一的对话角色，采集与优化是同一角色的两种姿态（对话），按"信息够不够"灵活跳。带 Grill 精神：主动追问、一次一个问题、给推荐答案、帮用户探究需求，但不替用户做决定。**自己问自己记**——用工具调用（`record_facts`）写事实，判断在 agent、处理在代码。
_Avoid_: 采集 agent + 优化 agent（两个）、聊天机器人、记录员、事实整理 agent

**前台接待员 (receptionist)**:
简历 agent 的角色定位——**单对话 agent 通过工具调起既有简历图**（生成/改写/开始改），不自己干重活。装备 = todo 动作级规划 + 按需技能 + 循环内摘要三件套；**不拆子 agent**（subagent 留作未来门，暂不用）。区别于"执行单元"：接待员面对的是多步、跨场景的对话任务。
_Avoid_: 执行单元、子 agent 分工（当前不做）

**技能 (skill)**:
按需加载的领域指令块（`SKILL.md`），**渐进披露**——会话开始只注技能索引（name+description），模型判断"现在用到哪块"才读全文。与**工具正交**：工具（`tools=`）是全局始终可用的能力，技能是"什么时候、怎么用"的说明书，工具不被技能收纳。四块拆分：**采集**（收录判定+矛盾嗅探）/ **优化**（建议六类+力度克制）/ **方向**（refine/commit/查市场）/ **grill**（追问技法，跨场景可复用）。面板/工具说明/四段注入（facts/偏好/简历/面板）留在 base prompt 每轮注入，不进技能。
_Avoid_: 工具收纳进技能、把面板规则塞进某场景技能

**grill 技法 (grilling)**:
跨场景可复用的追问技法——一次只问一个问题、给推荐答案帮用户省力、层层深挖不满足于"有"、够用即停、不替用户做决定。独立成 skill，不挂在"采集"下（采集只是它用得最多的场景之一；探需求/破僵局/聊矛盾也用得上）。
_Avoid_: 采集私有话术、只属于简历 agent 的精神

**生成简历**:
从事实库取材、整理渲染出新版简历的收口动作。触发权双通道：agent 自主提议 + 用户显式要求。**不是对话姿态**——生成由「文档任务」执行，被对话 agent 调起、service 落库，产出落版本化预览。
_Avoid_: 导出、渲染（作为触发概念）

**会话 (session)**:
每个简历版本一个对话会话。版本变更（生成/回滚/应用建议）都是边界，各开新 session；旧 session 只读（版本面板可看当时聊天+文档），不可续聊。当前 session = id 最大那个。首个会话（尚无简历）document_id 为 NULL（种子会话）。**UI 连续显示所有 session 历史（版本变更处有分隔标记），但发送序列只含当前 session**。跨 session 记忆由 active facts 承担，不靠聊天记录。
_Avoid_: 线程、分支（会话是线性的，不是可逆的编辑现场）

**上下文构成**:
发给对话 agent 的每轮输入 = active facts 全量 + 偏好 + 当前完整简历 + 面板四栏，外加图内 checkpointer 恢复的对话历史。**对话原文不剪枝**——UI 一致完整历史存 `chat_messages`（UI 真相源），agent 的工作记忆存图 checkpointer；上下文有界靠「版本切 session」（版本变更即新 session 自然收口）。超窗口由 deep agent 的 `SummarizationMiddleware` 在图内折叠（不落业务库）。
_Avoid_: 剪枝、向量检索注入（单用户几十条事实，全量注入即可）

**自己问自己记**:
对话 agent 用工具调用（`record_facts`）自主决定何时记什么——判断（值不值得记、收录判定）在 agent，处理（冲突/去重、落库）在代码（service 层 `confirm_facts`）。不设额外的"事实整理 agent"层——冲突/去重是确定性逻辑，不该用 LLM 重做。
_Avoid_: 后台再抽一次、事实整理 agent

**文档任务 (document task)**:
非对话的文档生产动作——整份生成 + 增量改写。被对话 agent 调起、service 层执行（写库/版本化），产出落版本化预览。1.1 的后台抽取就是"独立非对话任务被上传调起"的同构先例。
_Avoid_: 生成 agent（它不对话）、对话姿态

**优化点面板 (suggestion panel)**:
1.2 优化点的持有形态（**2026-08-13 定稿四栏，2026-09-23 迁到复合表**——组件仍叫 `SuggestionBasket`）。**四栏**：左 = 待定 : 已拒绝，右 = 已确认 : 正在聊；顶栏「优化点」工具坞胶囊展开浮窗。**栏内按「原因」分组**——优化点 = **改动记录**（`change_records`：`reason` 为什么 + `changes[]` 改什么，子项各自带状态），同原因的多条改动聚在一条记录下、不重复抄原因。跨页/刷新/回看都在。四条状态动作（接受 / 拒绝 / 聊一聊 / 撤回）+「开始改」**用户和 agent 都能操作**（§「优化点操作权」），**操作粒度 = 单条子项**；「开始改」应用已确认的子项 → 增量改写 → vN+1。
_Avoid_: 一次性建议列表、只读建议（面板是积累态，不是当轮消费）、右缘单卡片（旧形态，已取代）、扁平建议表（旧结构，2026-09-23 已被复合表取代）

**产物链 (resume JSON → HTML → PDF)**:
生成产物的三层形态（**2026-08-28 排版转向后**）。`resume_json` = 内容层（结构化成品真身，LLM 从事实库组合产出，消费方吃 JSON）；HTML = 渲染层（固定模板 Jinja2 确定性渲染的快照，`HTML = render(resume_json, typography)` 是纯函数）；PDF = 导出层（Electron `printToPDF`，已实现）。**上传 v1 是另一条**：Markdown（MarkItDown 抽取物）→ 元件原生预览，无 JSON。二阶段试衣间（换背景/贴照片）仍线头（长线）。
_Avoid_: 一次性生成带背景的成品（内容与视觉不该绑定）、LLM 排版（已退出）、Markdown 当生成版内容层（旧双源分层，已被 JSON 取代）

**优化建议 (suggestion)**:
1.2 对话 agent 的"优化"姿态产出——结构化建议（六类：quantify 量化 / word_choice 用词 / structure 结构 / fill_gap 补缺 / job_relevance 岗位相关 / highlight 卖点），每条带 target（Markdown 锚点定位）/original/suggested/reason/severity。确认/拒绝/多选；拒绝记忆 = 偏好表记"拒掉这一类"。**是「处方」物种**（见下），**主要由「未处理」分析产出**。
_Avoid_: 改动 diff（它是建议，不是已应用的改动）

**处方 vs 嘱咐 (prescription vs advice)**:
投递页分析产出的两种物种（**2026-09-13 定，用户医嘱类比**）。**处方** = 可执行、要被追踪、有生命周期（待收录 → 收录进优化点 → 接受/拒绝 → 应用）的建议 = 「优化建议」；收录进优化点面板（落在同一条改动记录上），之后归优化点状态机管。**嘱咐** = 有参考价值但不被追踪的结论（"投了很久没回音的共性是…"、"面这家可以多准备 X"），是「分析报告」的一部分，**没有状态、无处收录**。
医生开药 vs 嘱咐多喝水——两种层面，别混：**开了药你得复诊**（有状态），**嘱咐过就过去了**（随报告）。
_Avoid_: 把嘱咐做成可收录的建议（污染优化点）、把处方只当报告一部分（丢了它的生命周期/状态）

**分析报告 (analysis report)**:
投递页分析的整体叙事产物——一段可读结论（未处理 = 批次总览 / 已投递 = 截至昨天的日报 / 面试 = 单场准备），**装嘱咐不装处方**。按触发条件缓存、**算一次存一次**（切页读缓存，不重算）。**已投递的日报截至「昨天」、不以今天为窗**——昨天已过去、报告是日期的纯函数，故永不需复算。
_Avoid_: 把报告拆成一条条建议、每次进页面重算、拿报告当简历改动清单

**优化点操作权 (suggestion agency)**:
agent 对优化点面板的操作权边界（2026-08-25 定稿）——决定权在用户 = **拍板**在用户，不是"操作权只能在鼠标"。三条：看出该改要**主动说**（推荐义务）；动手前**先问**（得准才动手）；用户表态后（口头 or 面板）**直接执行**。四条状态动作（接受/拒绝/聊一聊/撤回）+「开始改」都开放给 agent（`set_change_status` 工具 + 两个「开始改」入口——聊天说 / 面板按钮，2026-09-09 起都触发同一条简历图、过人门）；聊天是建议唯一入口，不设手动填表；卡片只在「有产物要用户过目、且面板承载不了」时出现。详见 resume.md §12.6。
_Avoid_: 操作权只在鼠标、agent 只读面板、被动等指令

**判定标准**:
判断"素材够不够、简历好不好"的依据。只有基础信息走硬判（本地比对），量化/用词/结构/卖点等语义项全交 LLM。标准是"权威默认 − 用户拒绝项 + 用户自定义"，不是写死的清单。
_Avoid_: 最小信息集、必填清单、以全为好

**偏好 (preference)**:
用户对判定标准的个人意志——拒绝项（"不量化""不提供薪资"）与自定义标准（"突出项目而非学历"）。与事实同生命周期（聊出来→确认→独立表持久化→设置页可编辑），但独立于事实库。优化建议被拒 = 记"拒掉这一类"，同类不再建议。
_Avoid_: 黑名单、设置项（它是业务数据不是应用配置）

**基础信息**:
简历的客观事实项（联系方式/邮箱/学历/时间等），唯一走硬判的部分。缺哪项拿本地事实库比对，没有再问用户；聊天生成时先填这张表再聊别的。
_Avoid_: 必填项、硬性门槛

**确认卡片**:
**已取消（2026-08-05 重构）**，**以「抽取信息卡片」轻形态回归（2026-08-06 定稿）**——见上方「抽取信息卡片」。
_Avoid_: 审批单、只读确认框

**总分两段式抽取**:
抽取用「总（entry）+ 分（child）」两段式——工作/教育/项目/技能这类「多段并列」内容，先抽「总」（公司+岗位+时间段一句话），再逐条搬其子要点。保内容**结构性 + 完整性**（不丢归属、不丢细节）。
_Avoid_: 平铺（一段经历散成 N 点）、压缩（多条合并成一条）

**封闭通道（简历上传）**:
简历只传一份。上传/抽取完成（ready）后上传入口全关；换简历 = 显式「重置」→ 回到空态再传新 v1。解析中可反悔（可取消/重传，旧抽取作废）。
_Avoid_: 反复传简历（打断思路循环、扰乱判断）

**统一冲突检测**:
事实**入库前**（确认 / 对话挖掘 / 手填）统一检测——同一实体 + 内容不同（真伪冲突）→ 新 active 旧 superseded 留 history；内容相同/近似（幂等）→ 跳过；不同实体 → 直接写。重语义矛盾留给对话澄清。
_Avoid_: 事后对账（作为唯一手段）、以全为好

**一阶段 / 二阶段（简历生成）**:
一阶段只做简历的**内容+排版**（一页纸、白/透明底、预留证件照空位）；二阶段是用户在左栏**试衣间**里 DIY 视觉皮肤（换背景、贴证件照）。两阶段解耦——HTML 会拉伸，背景/照片是位图，不该跟内容绑定生成导出。
_Avoid_: 一次性生成带背景的成品

**试衣间（简历）**:
左栏简历展示区的升级形态——不只是预览，还能让用户给简历换背景、贴证件照（二阶段加工）。
_Avoid_: 预览器（仅看）

**统一入口（文件上传）**:
所有简历文件从一个入口进，按格式分派到对应提取方式，统一转成文本喂给 LLM。只收 TXT/MD/DOCX/PDF 等白名单格式（按扩展名拦截，**未启用 magika 内容嗅探**）；提取不出的（如扫描件 PDF）在提取后弹窗提示。
_Avoid_: 多入口、统一转换器（Tika/LibreOffice 太重）

**扫描件 PDF**:
图片型 PDF——页面是图片、没有文本层，本地抽不出文字。处理：抽取时检测文本量，抽不出来就弹窗提示用户换文本型或手打（1.1 不做 OCR，**已砍 2026-08-31**）。**多模态转写曾于 2026-08-26 探讨、暂缓**（工程量大、收益边际；DeepSeek 视觉是独立模型 `deepseek-v4-flash-vision-exp`，与文本模型分家，需单开配置槽位 + PDF 栅格化，先不做）。
_Avoid_: 图片简历（口语说法）

**MarkItDown**:
微软开源的文件→Markdown 编排层。按格式分派到底层专精小库（pdfplumber/mammoth 等）、统一吐 Markdown 喂 LLM；charset-normalizer 编码嗅探内置。我们用它做上传文件的统一提取层（装 `[pdf,docx,pptx,xlsx]`，不装音频/视频/Azure 等重 extras）。**调用前按扩展名白名单拦截，未启用其 magika 内容识别。**
_Avoid_: 自己写按扩展名分派（重复造轮子）

**左栏预览分档**:
上传文件在左栏的显示策略——能渲染原件的显示原件（PDF/TXT/MD/HTML），渲染不了的 Office 格式（DOCX/PPTX/XLSX）显示解析后的 Markdown 兜底。预览能力与解析能力解耦。
_Avoid_: 只能预览 PDF（太窄）、统一只显示文本（丢失原件排版）

**工具台骨架 (workbench)**:
两种页面骨架之一。**加工一个对象**的页面：左对象右助手，无页头，分栏面板各自内部滚动（如简历页：左简历预览 + 右聊天）。另一骨架是「文档页骨架」。
_Avoid_: 分栏布局（口语，没说清语义是"加工一个对象"）

**文档页骨架 (document)**:
两种页面骨架之一。**浏览/管理一串条目**的页面：页头 h1+副标题，单列文档流，外层滚动（如设置页、投递页）。另一骨架是「工具台骨架」。判定：加工一个对象→工具台；扫一串条目→文档页。
_Avoid_: 列表页（太窄，设置页也属此骨架）

**工具坞 (ToolTab)**:
「标签 pill + 点击展开浮窗」的唯一浮窗原语（优化点/资料集/检索控制台共用）。触发标签 = 实底工具按钮；浮窗 = 玻璃 `.tool-panel`，fixed 视口定位、点外收起，支持左/右锚点（align）。**不为单个工具单开第二条浮窗路。**
_Avoid_: 各工具自绘浮窗、胶囊触发器

**排版双控件 (typography controls)**:
简历预览的两个独立控件（**2026-09-02 定稿，拆开旧的单一 `zoom` 伪概念**）——**控件 A · 排版参数（数据层）**：字号 / 行距 / 段距 / 字间距 / 栏距（`Typography` 五参数），一体注入模板 `:root` CSS 变量，**落库挂版本**、新生成稿继承上一版、导出 PDF 吃当前版；改时防抖回后端 `POST /documents/current/typography` 重渲染。**控件 B · 整页缩放（视图层）**：用 CSS `zoom` 把 A4 纸等比缩进预览窗看全貌（打开自动适应宽度），仅本次启动内存态、与导出无关。**两者都只对生成的简历生效**，用户上传原件都隐藏。
_Avoid_: 单一 `zoom` 兼任两者（旧伪概念，已拆）、字号参数不落库（只在预览 iframe 生效、导出吃不到）、`transform:scale` 充当整页缩放

**系统气泡 (system bubble)**:
agent/系统的**非对话动作**在聊天框里的统一呈现——区别于聊天气泡（user 右 / assistant 左）的一组专属样式。所有系统动作都走它，**不落进聊天气泡伪装成"助手说话"**。分**两档在用**：**事件**（灰条）= 纯文本成功事件（上传/确认/回滚/方向变更/已停止/「已放 N 条建议」）；**错误**（红条）= 纯文本失败与约束报错（上传失败/抽取失败/连不上后端/各种操作失败）。第三档「**系统卡片**」= 带按钮的结构化块，视觉从助手侧**拉开**——**档位保留但已无产品实例**：事实冲突卡 2026-09-09 改对话内反问、取消；「还没有结构化简历→去生成」卡 2026-09-08 删（后零文档也能生成，死路已消解）。**所有事件/错误都留痕落库**（写进 `chat_messages`，刷新后仍在）——记气泡的权责统一在**后端**（成功事件与失败事件同一真相源，不再是"成功后端记、失败前端拼"的双源）。
_Avoid_: 系统提示写进聊天内容、成功/失败两处真相源、错误只 toast 不留痕

**应急生成口 (escape-hatch generation)**:
**已作废（2026-09-08）**——曾指「去生成」系统卡片上的一键动作：上传原件还没有结构化简历时，绕开对话 agent 直调 `generate_preview` 产出一版暂存预览。 合并出简历流水线后，**零文档也能从 facts 冷启动生成**（「开始改/改简历」不再撞死路），这个应急逃生口连同它的卡片一并删除。生成入口现统一为：对话 agent 调 `generate_resume` 工具，或「开始改」按钮——都走同一条图 + 人门确认。
_Avoid_: 再提「去生成」按钮、把冷启动当成需要逃生口的死路

## 分区设计文档索引

各功能分区的具体决策、设计与理由，见对应分区文档：

- **简历** → [`docs/design/resume.md`](docs/design/resume.md)：整体大图、生成触发、事实库、判定标准、文件提取、版本化回滚、一/二阶段+试衣间。
- **投递** → [`docs/design/apply.md`](docs/design/apply.md)：整体定位、来源适配器、数据模型、去重、排序、投递执行线头、方向、市场信号。

---

## 投递（阶段二）

> 领域模型随设计推进持续补充；决策细节与理由归 `docs/design/apply.md`。

**职位聚合 (job aggregation)**:
从多个招聘平台抓取岗位、规范化成统一 schema、收进一个**跨平台统一列表**。产品定位 = 发现 + 统一入口 + **用户保留决定权**（用户筛、用户投、用户跟）。不是"代投"，也不是"记录管理"。
_Avoid_: 海投工具、投递记录管理系统

**来源适配器 (connector)**:
每个招聘平台一个，`fetch_jobs(query) -> RawJobRecord[]`，把平台字段归一化成统一 schema。走两种路之一：**免登录 JSON API**（纯 HTTP 不登录，如猎聘经后端 `curl_cffi`）或 **Electron 隐藏窗口读 DOM**（`BrowserWindow` 加载搜索页、复用登录态、读 DOM 抓卡片——两家 DOM 平台走这条）。岗位数据**归一化成统一 schema**，用户保留决定权。实现 → apply.md §3。**注**：BOSS 已彻底砍（2026-08-31）、智联已放弃；现存 = 猎聘 + 前程无忧。
_Avoid_: 全自动海投、单一技术栈（只爬或只 API）

**岗位生死 (liveness)**:
岗位还招不招的**客观状态**，由**程序**从平台更新得知——`active` 在招 / `stale` 不活跃 / `closed` 关闭 / `unknown` 未知。独立于用户动作。
_Avoid_: 已投/已拒混进 liveness

**岗位筛选反馈 (job screening)**:
**已作废（2026-08-30）**——「投了/没投 + 原因」这个概念被**后续状态**吸收：投了 = 状态 `applied`，看了不合适没投 = 主动放弃（`withdrawn`），岗位关了 = `job_closed`。旧表 `job_screening` 清空重来，取代为 `job_followup_events` 时间线。见 `docs/design/apply.md` §12。
_Avoid_: 已投布尔 + 没投原因分表（旧模型，已作废）

**岗位去重 (job dedup)**:
**只做同源去重**：`source_name + external_id`（平台岗位 ID）语义唯一、幂等 upsert，不新增重复行（**应用层 select-then-insert 兜底，非数据库约束**）。**跨源不做去重**——同一岗位在猎聘和前程无忧都出现就是两条，用户想投哪个平台投哪个。也不做跨源语义聚合、不做字段强归一化（薪资/城市存原始字符串）。增量 = upsert 只更新岗位字段，**不动用户反馈**。细节 → apply.md §5。
_Avoid_: 跨源去重/聚合、物理合并多平台同岗位、强归一化字段、每次抓都清空重铺、用 URL 做跨源去重

**岗位时间戳 (job timestamps)**:
岗位记录的两个时间——`platform_updated_at`（平台自己的发布/更新时间，**可空**：有的平台不公开）与 `observed_at` / `last_seen_at`（**我们自己的抓取时间，恒有**）。新鲜度排序的地基 = 抓取时间 + liveness；平台发布时间拿到是加分信号，不能当地基。
_Avoid_: 用不可靠的平台发布时间做新鲜度排序

**岗位更新触发 (job refresh)**:
岗位怎么被重新抓取。**2026-08-31 定稿重构**：**事件驱动**——前端在用户动作（推进岗位 / 改方向 / 进入投递页 / 开开关）触发 `kickCrawl`，Electron re-entrancy 闸 + 后端 `should_crawl` 门控；**废 15 分钟轮巡 + 废手动刷新按钮**。零计时器：throttled/exhausted 靠「事件到来时惰性看钟」，不用 setTimeout/setInterval 主动唤醒。
_Avoid_: 高频后台抓取（反爬、限流）、手动「立即刷新」按钮

**续页游标 (crawl cursor)**:
抓取进度的指针——每个平台记「当前翻页游标」，一轮抓完推进、下一轮接着走（不重扫第 1 页）。统一存后端 SQLite `crawl_state` 表（每平台一行），跨重启/跨天不丢。**游标是二维的 `(组下标, 组内页号)`**：方向是 `keywords: list[list[str]]` 二维查询组，每组各自有第 1..K 页、组间顺序消费（组 1 走完再轮组 2），没有扁平页号。**两平台翻页机制各异**（实测 2026-08-31）：猎聘 `currentPage` 真分页（0 起）、前程无忧点翻页器 `.el-pagination .btn-next` 触发 XHR `pageNum`（pageNum 在 XHR 不在 URL）——本质都收敛到「一次请求的参数」，游标记每平台**原生页号**（猎聘 0 起、前程无忧 1 起）。改方向 / 核爆 → 游标归零。
_Avoid_: 从第 1 页重爬、扁平页号（忽略查询组维度）

**抓取三态 (crawl three-state)**:
一轮抓完归入三态之一：`fresh`（有新增，游标前进）/ `throttled`（被限流，记失败时间戳 + 惰性冷却，默认 15 分钟）/ `exhausted`（空页，按日期打标记当天跳过、转天重查）。**三态是查询组级**（exhausted/throttled 记到组粒度）——组 1 空页 = 组 1 exhausted、游标移到组 2；**所有组都到底才整家 exhausted**。**exhausted = 空页**，不是「0 新岗」——「有卡片但全被去重」该继续翻页，不是到底。与「撞 cap 仍有货」是两回事。
_Avoid_: 把「0 新岗」当 exhausted、throttle 靠事件计数退避、三态记到平台级（会噎死后续查询组）

**深爬 / 稳态 (deep-crawl / steady-state)**:
**不是两个阶段，是同一个循环跑出来的自然现象**——库里空时游标从第 1 页往后走、每页都是新岗（看着像深爬攒量）；游标绕满一圈回到第 1 页后，去重滤掉老岗、只剩真正新帖（看着像稳态盯新）。**无阶段存储、无过渡事件**，只一个 `cap`（扫几页上限）由用户手动调。
_Avoid_: 两阶段状态机、`max_pages` + 稳态缩窗页数两个参数

**触发值 (crawl threshold)**:
「保留的未处理底仓」——`unprocessed ≤ 触发值` 时轮到补下一批（默认 5）。触发靠**状态跃迁检测**（`unprocessed` 从 ≥ 触发值 跃迁到 < 触发值才踢一次），不是每次 `refreshList` 都踢，避免切 tab/改排序的 UI 抖动变成高频抓取。
_Avoid_: backlog 清零才补（默认 0 空仓）

**投递 (apply)**:
用户选岗后去投的那一下。阶段二核心 = 手动投（内部窗口）+ 记录追踪；自动化（预填/点投递/自动抓结果）是 L2/L3 渐进升级线头。**手动挡基线（2026-08-30）**。见 `docs/design/apply.md` §12。
_Avoid_: 全自动代投、投递包

**跟进 (follow-up)**:
已投之后推着岗位往下走——一条状态线（已投递 → 面试 → 录用 / 不再追踪）。**手动挡基线（2026-08-30）**：用户手动标状态、应用不主动催；自动化读平台私密态仍是线头。见 `docs/design/apply.md` §12。
_Avoid_: 试图自动化读平台私密态

**v1 范围（阶段二）**:
岗位聚合（抓取 → 规范化 → 跨平台列表）+ **投递跟进状态线**（`job_followup_events`：已投递 → 面试 → 录用 / 不再追踪）+ **面试日历**（`interviews` 场次编年）。投递执行自动化（预填/自动投递/自动抓结果）→ 线头。connector 现状：猎聘（免登录 API）+ 前程无忧（读 DOM）。智联已放弃、**BOSS 已彻底移除**（抓取/登录/筛选/类型/历史数据全清，2026-08-31）。**注**：旧的「岗位筛选反馈」（`job_screening` 表，投了/没投布尔 + 原因枚举）已作废（2026-08-30），被状态线取代。边界与取舍 → apply.md §2。

**岗位详情 (job detail)**:
点击岗位在**应用内**打开真实岗位页（Electron 独立窗口内嵌真浏览器，非 iframe——平台设了 X-Frame-Options 反嵌）。**交互（2026-08-30 定）**：未处理 tab 的卡片 hover 蒙「点击查看详情」玻璃 → 点击直接开内置窗口跳岗位 URL（**砍中间详情弹窗**）→ 关窗口弹选项框标「投了/没投（原因）」。**这个窗口就是未来投递执行引擎的地基**。
_Avoid_: iframe 嵌岗位页、跳外部浏览器当唯一形态、点卡片先弹详情弹窗

**岗位关闭真相源 (job closure truth source)**:
岗位是否"关了"的有效状态 = **用户确认关闭（权威，当前真相源）** ∪ **平台 liveness（候选信号，暂不可信）**。**灰度 = 候选信号不可信；藏掉 = 可信/用户权威确认**——显示规则随真相源演进。细节 → apply.md §6bis。
_Avoid_: 直接信任平台 liveness 隐藏岗位、把用户确认关闭混进平台 liveness

**相关度排序（字段级）**:
**已下线（2026-09-08）**——岗位列表的「相关度」曾是**字段级关键词匹配**（标题/技能/标签含关键词即算相关，SQL 字段匹配，不进 LLM）。但它依赖 `GET /jobs?query=` 传检索关键词，**前端从无搜索框、从不传 query** → 相关度排序退化成鲜活度，是死维度，故随排序收敛一并砍掉。检索关键词未来从简历+事实库提炼（线头），届时作为**新排序维度**重新加入控件，不是复活旧开关。**LLM 打分早已砍（2026-08-31）**——除猎聘外不打开详情页就拿不到详情，无从匹配。
_Avoid_: 把 LLM 打分排序拉进 v1、关键词来源硬编码、留一个不接输入源的"相关度"空开关
**岗位列表操作 (job list controls)**:
**已收敛（2026-09-08）**——砍掉排序维度选择，全 tab 只留「时间」一个维度，每 tab 一个**语义时间键**（未处理=收录时间 `last_seen_at`、已投递=投递时间、录用=录用时间、不再追踪=处理时间，后三者=当前态事件的 `at`）；排序与日期范围筛选共用同一键。面试 tab 是纯日历、无此控件。**统一控件** = `[语义时间文字标签] [时间范围筛选器（固定宽度）] [正倒序箭头]`，四列表 tab 复用、只换前缀标签词。鲜活度/相关度两维度已砍（相关度本是死维度——前端不传 query，退化成鲜活度）。
_Avoid_: 鲜活度/相关度维度选择器（已砍 2026-09-08）、LLM 相关度打分排序进 v1（已砍 2026-08-31）

**方向 (search direction)**:
投递与简历之间的**桥梁**——「求职目标」是派生事实（存 `user_facts`，source=chat），「搜索词」是从目标**派生**的翻译（二维查询组，组内 AND、组间 OR）。是**决策**（人拍板），不是推导（facts 变了不自动重算）。v1 一次一方向。改方向 = **口头化**（2026-08-25）：agent 口头问 + 用户口头答 + `commit_direction` 落库；**岗位处置与方向落定拆分**——commit 只写方向返回待处置未处理岗位数，后端卡门 + 前端就地弹「留/删」。级联 → apply.md §11.2 系列。
**城市挂在查询组上（2026-09-13）**：方向**没有**单值城市字段，城市是每个查询组的属性，与组一一对应（`Direction(role, keywords, cities)`，`cities` 与 `keywords` 平行等长）。`["Java","后端"]` 配成都、`["产品"]` 配杭州各搜各的。**城市逐组必填**（无「全国」——前程无忧留空=回落 IP 城市），`len` 相等 + 逐项 25 城白名单落定前双校验出声。
_Avoid_: 方向级单值城市（旧模型，2026-09-01–2026-09-13 的止血态，已取代）、「方向多选城市」（探针数 = 组数 × 城市数，会顶翻 封顶）、搜索关键词（口语，混了搜索词与技能标签）、方向 = 自动推导产物、独立配置表、方向提案卡片（已废）

**检索语义 (search semantics)**:
招聘平台搜索框对关键词的**实际处理规则**（2026-09-11 实测）——**自适应 AND**（多词有交集时按 AND，交集不足时**静默放宽**成 OR）、放宽时留下的词**与词序无关且不可控**、平台**自带变体匹配**（搜 `vue` 覆盖 vue2/vue3/vue.js）。由此推出写法：一组 = 一个概念 + 一个与它**大范围共现**的收窄词，**同义词/变体只留最强的一个**，**不用低共现词收窄**。2~3 组起步，岗位变少后扩组（上限 6）。
_Avoid_: 组内 AND（旧假设，已实测证伪——放宽让它不可靠）、词序优先（实测与顺序无关）、同义词堆一组（平台自己会扩，白费功）

**市场信号 (market signal)**:
**已砍（2026-08-31）**——曾设想从 jobs 表聚合「你的机会集」（岗位量/薪资分布/技能门槛/城市分布），摆方向面板做错位对照。但从未实现，且被 `query_market` 取代：判断方向这类需求由 agent 现场查猎聘即查即弃承担，落库画像/独立分析管线是过度设计。市场调研唯一保留的线头 = 低频广度探测（回答「哪个方向在涨」）。
_Avoid_: 全市场格局、自动改方向的触发器

**查市场 (query_market)**:
简历 agent 的一个工具——现场调猎聘接口查市场客观统计（岗位量 / 城市 / 学历 / 年限 / 薪资段），**只吐原始统计、不做判断**；判断（你 vs 市场缺什么）由 agent 拿统计比对已注入的 facts 后自己讲出。样本仅猎聘，诚实标注。设计 → 。
_Avoid_: 市场分析报告、独立分析引擎、预取的画像缓存

**市场快照 (market snapshot)**:
「查市场」工具返回的客观统计产物——纯数字事实（岗位量 / 城市分布 / 学历年限 / 薪资段）。**现场查即弃，不落库、不缓存**。是证据不是结论，仅猎聘样本。
_Avoid_: 市场画像、落库的派生缓存

**市场调研的作用域 (market research scope)**:
`query_market` **只在「还没有大致方向、去摸行情」的阶段有用**——产出的是**供给侧信号**（市面存在什么岗位、招聘方管它叫什么、在哪些城市、量级如何），用来把用户嘴里的词翻译成招聘方的词。**不能**用来预测投递效果（那是反馈量纲，工具手里没有），也**不能**佐证检索词的好坏（标题命中率是代理指标，见 ；平台恒返固定条数、无判别力，且命中可能发生在 JD 正文而我们只数得到标题）。检索词的真实好坏只能由**投递反馈**回头验，不能由本工具回答。
_Avoid_: 用市场工具验检索词 / 预测投递效果、把「标题命中率」当检索词优劣的证据、拿平台返回条数当市场规模的判别信号

**后续状态 (follow-up status)**:
一个岗位投出去之后的进展。一条线性状态线：`applied` 已投递 → `interviewing` 待面试 / `interviewed` 已面试 → `offered` 录用，负向终态 `not_pursuing` 不再追踪。**起点「空着」 = 未处理**（不是状态值，是无记录）。任意跳、可回退。**状态 = 已完成的事实，不是进行时**。
_Avoid_: 单向漏斗、硬状态机、把「没投」当一个状态

**状态时间线 (status timeline)**:
后续状态的唯一真相源——追加式事件表 `job_followup_events`，**当前态 = 该岗位最新一条事件**（派生，不建快照列）。每次状态变化追加一行，可回退 + 留历史（「被拒又捞回」这种市场信号不丢）。
_Avoid_: 当前态快照列 + 历史日志两处真相、状态机校验

**未处理 (unprocessed)**:
无任何状态事件的岗位——刚爬进来还没看。是后续状态的**初始态**（空着 = 未处理），不是「没投」（「没投」概念已并入主动放弃）。改方向时只有这批跟方向绑定、可清可留。
_Avoid_: 没投、待处理（作为「决定不投」的态）

**不再追踪 (not_pursuing)**:
后续状态的**负向终态**——不再往下追了。内部分类 `reason`：`withdrawn` 主动放弃 / `failed` 未通过 / `job_closed` 岗位关闭 / `duplicate` 已投过（同一岗已在别处投过）。加 `note` 自由文本兜底。
_Avoid_: 未通过/放弃各开一个状态、把岗位关闭单开客观生死轴

**主动放弃 (withdrawn)**:
「不再追踪」的一种原因——**我主动不追了**（看了不合适 / 聊着聊着不合适 / 能力不到）。与未通过（对方没让我过）是两码事、两种复盘信号。来源 = 旧 `job_screening` 的 mismatch/not_suitable/underqualified 塌缩。
_Avoid_: 没投（旧概念）、不合适

**面试阶段 (interviewing stage)**:
后续状态里「面试」这一段——**拆两个状态值**：`interviewing` **待面试**（已排、约定时间未到）与 `interviewed` **已面试**（约定时间已过，**惰性派生态**，读时按 `now ≥ scheduled_at` 现算）。一个岗位可以有 0..N 场面试，但状态线只在「非面试 → 面试」与「面试 → 终局」这类事实跃迁时追加，不一场一事件。
_Avoid_: 电话面/技术面/HR面子状态枚举、「有回音」独立态（已砍）、「面试中」进行时态

**面试场次 (interview round)**:
一次具体的约面——新建表 `interviews`，一个岗位 0..N 行，带 `scheduled_at`（具体时间点）+ `location`（自由文本，可空）+ `outcome`（结果）+ `next_round_id`（显式链串轮次）。是**编年**，日历直接读全表、不过滤任何状态。**跟「面试阶段」是两层**：阶段是岗位在流水线的位置，场次是那一段里的每次约面。
_Avoid_: 面试记录（丢了「约面」的排期语义）、跟「面试阶段」混为一谈

**面试场次结果 (interview outcome)**:
一次面试场次走到哪一步——5 档线性生命周期：`scheduled` 待面 → `awaiting` 等结果 → `next_round` 下一场 / `offered` 录用 / `failed` 未通过。**只有终局（录用 / 未通过）条件式地驱动岗位状态线**，其余不联动。**「默拒」不是状态**（投了没回音 = 停「已投递」，面了等消息 = 停「等结果」）；**无「取消」档**（没面成要么改期、要么 = 未通过）。
_Avoid_: 通过/未通过二元、被拒（未通过这词更委婉，rejected→failed）、取消（无此态）

**投递动作 (application attempt)**:
**线头（v1 砍掉）**——「这一次投递」的成败/败因（成功 / 失败-已投过 / 失败-错误）。是 L2/L3 自动化（应用驱动投递）才需要记录的；手动挡里「我投了吗」由人直接标「已投」，没有「这次动作」要记。
_Avoid_: 与后续状态混为一谈（动作 vs 进展是两轴）
